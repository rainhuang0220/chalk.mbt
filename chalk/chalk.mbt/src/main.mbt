// MoonBit Chalk 终端着色与样式库
// 功能与 Node.js 的 chalk 等价

import "./chalk"

// 导出公共API
pub let chalk = chalk
pub let strip_ansi = chalk::strip_ansi
pub let visible_width = chalk::visible_width
pub let supports_color = chalk::supports_color
pub let enable = chalk::enable
pub let disable = chalk::disable
pub let theme = chalk::theme
